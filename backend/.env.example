# ============================================
# Backend Local Development Environment Variables
# ============================================
# This file is for running the backend locally (not in Docker)
# Copy this to .env in the backend folder

# ============================================
# REQUIRED: Database Configuration
# ============================================
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# For local PostgreSQL:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/validquery

# For Docker PostgreSQL (if using docker-compose for DB only):
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/validquery

# ============================================
# REQUIRED: OpenAI API Configuration
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI model to use (default: gpt-4)
# Options: gpt-4, gpt-4-turbo-preview, gpt-3.5-turbo
OPENAI_MODEL=gpt-4

# ============================================
# Query Execution Configuration
# ============================================
# Query execution timeout in seconds (default: 30)
STATEMENT_TIMEOUT=30

# Maximum number of rows returned per query (default: 100)
MAX_QUERY_LIMIT=100

# ============================================
# API Configuration
# ============================================
# API host (default: 0.0.0.0)
API_HOST=0.0.0.0

# API port (default: 8000)
API_PORT=8000

# ============================================
# CORS Configuration
# ============================================
# CORS origins (comma-separated list)
# For local development with Vite frontend:
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# For production or Docker:
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
